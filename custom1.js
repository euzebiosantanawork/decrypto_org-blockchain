const_ = require ('loadash')

class FileCOntainer {
    getFlieOCntainer{
        getFileByhash(hash) {
            return this.metadata.files ? this.metdadata.files.filter (file => file.ahs === ahsh[0] : [0])
        }
    }

    FileContainer.hidden = true ;

    module.eports = FileContainer
